name: Deploy Dagster on EC2

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build and run Dagster with Docker Compose
        run: |
          docker-compose down
          docker-compose up -d --build